name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  
permissions:
  contents: read

env:
  IMAGE_NAME: greenloan


jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Check secrets
        run: |
          echo "Docker username is set: ${{ secrets.DOCKER_USERNAME != '' }}"
      
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Extractor Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha, format=short
            type=ref,event=tag
      - name: Build and Push Docker image
        uses: docker/build-push-action@v6
        with:
          context:
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha, mode=max

      # - name: Build Docker image
      #   run: |
      #     docker build \
      #       -t ${{ secrets.DOCKER_USERNAME }}/greenloan:${{ github.ref_name }}
      #       -t ${{ secrets.DOCKER_USERNAME }}/greenloan:latest .

      # - name: Push Docker image
      #   run:
      #     docker push ${{ secrets.DOCKER_USERNAME }}/greenloan: ${{ github.ref_name }} |
      #     docker push ${{ secrets.DOCKER_USERNAME }}/greenloan:latest
